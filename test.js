// Throw if not on branch matching input flavor name?
// Or detect flavor name?


// GOAL: 2 new tags — flavor and flavor-vX.X.X — exist on the pal repo
// plain flavor name is same tag name, updated code (previous one
// should be deleted and replaced)

// Which flavor are we updating?
// What version are we updating to?
